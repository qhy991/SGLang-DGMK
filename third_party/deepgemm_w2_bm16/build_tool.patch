diff --git a/build_sgl_deep_gemm.sh b/build_sgl_deep_gemm.sh
index 7ca3b5e..fee8a70 100755
--- a/build_sgl_deep_gemm.sh
+++ b/build_sgl_deep_gemm.sh
@@ -96,7 +96,8 @@ assert helpers_spec.loader is not None
 helpers_spec.loader.exec_module(cuda_helpers)
 
 cuda_home = cuda_helpers.find_cuda_home()
-os.environ.setdefault('TVM_FFI_CUDA_ARCH_LIST', cuda_helpers.get_cuda_arch())
+if 'TVM_FFI_CUDA_ARCH_LIST' not in os.environ:
+    os.environ['TVM_FFI_CUDA_ARCH_LIST'] = cuda_helpers.get_cuda_arch()
 
 cxx_abi = int(torch.compiled_with_cxx11_abi())
 extra_cflags = [
@@ -147,11 +148,20 @@ print(f"Built {target}")
 PY
 
 echo "--- Installing build frontend ---"
-"$PYTHON_EXE" -m pip install --quiet --upgrade build
-
-echo "--- Building wheel ---"
-mkdir -p "$DIST_DIR"
-"$PYTHON_EXE" -m build --wheel "$BUILD_DIR" --outdir "$DIST_DIR"
+if [[ "${SGLANG_DEEPGEMM_BUILD_WHEEL:-0}" == "1" ]]; then
+  if ! "$PYTHON_EXE" -c 'import build' >/dev/null 2>&1; then
+    "$PYTHON_EXE" -m pip install --quiet build
+  fi
+  echo "--- Building wheel ---"
+  mkdir -p "$DIST_DIR"
+  "$PYTHON_EXE" -m build --wheel --no-isolation "$BUILD_DIR" --outdir "$DIST_DIR"
+else
+  echo "--- Skipping wheel (staged package is ready) ---"
+fi
 
 echo "--- Done ---"
-ls -lh "$DIST_DIR"/sgl_deep_gemm-*.whl 2>/dev/null || ls -lh "$DIST_DIR"/sgl-deep-gemm-*.whl 2>/dev/null || ls -lh "$DIST_DIR"/sgl_deep_gemm*.whl
+if [[ "${SGLANG_DEEPGEMM_BUILD_WHEEL:-0}" == "1" ]]; then
+  ls -lh "$DIST_DIR"/sgl_deep_gemm-*.whl 2>/dev/null || ls -lh "$DIST_DIR"/sgl-deep-gemm-*.whl 2>/dev/null || ls -lh "$DIST_DIR"/sgl_deep_gemm*.whl
+else
+  ls -lh "$PKG_DIR/_C.so"
+fi
